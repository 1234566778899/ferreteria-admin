import type { Tone } from "@/components/ui";

/** Color de la etiqueta por tipo de documento. */
export const kindTone: Record<string, Tone> = { entrada: "success", salida: "info", ajuste: "attention" };
